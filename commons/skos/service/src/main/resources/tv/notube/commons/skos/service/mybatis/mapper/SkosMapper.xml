<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="tv.notube.commons.skos.service.mybatis.mapper.SkosMapper">

    <select id="selectSkos" parameterType="map" resultType="java.lang.String">
        SELECT skos FROM skos_relations_2 WHERE entity = #{resource}
    </select>

</mapper>